import React from 'react';
import { Box, Typography } from '@mui/material';

function AdminDashboard() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Admin Dashboard (Em Construção)</Typography>
      <Typography variant="body1">Esta é a página do painel de controle do administrador.</Typography>
    </Box>
  );
}

export default AdminDashboard;