import React from 'react';
import { Box, Typography } from '@mui/material';

function AdminSettingsScreen() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Configurações Admin (Em Construção)</Typography>
      <Typography variant="body1">Esta é a página de configurações do administrador.</Typography>
    </Box>
  );
}

export default AdminSettingsScreen;